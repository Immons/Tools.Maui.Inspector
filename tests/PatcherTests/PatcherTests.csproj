<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <!-- Links the patcher sources directly — a plain console runner, no test framework:
       `dotnet run` prints PASS/FAIL per scenario and exits non-zero on any failure. -->
  <ItemGroup>
    <Compile Include="..\..\src\Immons.Tools.Maui.Inspector.Sync\XamlPatcher.cs" />
    <Compile Include="..\..\src\Immons.Tools.Maui.Inspector.Sync\XamlTagScanner.cs" />
  </ItemGroup>
</Project>
